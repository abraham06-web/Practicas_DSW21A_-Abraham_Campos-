fun main() {
    val notas = doubleArrayOf(7.0, 7.5, 7.0, 7.5, 7.0)

    var suma = 0.0
    for (nota in notas) {
        suma += nota
    }

    val promedio = suma / notas.size
    println("El promedio de las notas es: %.2f".format(promedio))
}
